name: Stub Generate Preview
on:
  push:
  workflow_dispatch:

jobs:
  push-scratch:
    name: Stub push scratch docs
    runs-on: ubuntu-latest  # or silabs-internal if you must test that runner
    timeout-minutes: 5
    steps:
      - name: Stub
        run: echo "stub ok"
